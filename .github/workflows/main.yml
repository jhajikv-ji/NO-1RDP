name: RDP
on:  workflow_dispatch
env:
  REPO: ${{ GITHUB_REPOSITORY }}
jobs:
  interactive:
    runs-on: ubuntu-latest  
    steps:     
      - name: new
        run: printf "root\nroot" | sudo passwd root && printf "root" | su - root -c " echo $REPO" 



